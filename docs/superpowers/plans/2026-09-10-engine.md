# Impel Down 엔진 구현 plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 규칙 파일(`rules.json`)을 읽어 지정 사이트·경로를 모든 브라우저에서 막는 mitmproxy 기반 엔진과 그 설치 스크립트를 만든다.

**Architecture:** 순수 Python 모듈(`impel/`)이 규칙 로드·주소 매칭·시간표 판정·hosts 블록·프록시 설정 파싱을 맡고, mitmproxy 애드온(`addon.py`) 하나가 그 모듈을 훅에 연결한다. 엔진은 root LaunchDaemon으로 상시 실행되며, 규칙 파일은 앱이 쓰고 엔진은 읽기만 한다.

**Tech Stack:** Python (시스템 3.9로 테스트, mitmproxy 독립 실행 파일 12.2.3 안의 Python으로 실행), pytest, mitmproxy 애드온 API (`tls_clienthello` · `request` · `running`), launchd, `networksetup`, `security`.

**Spec:** `docs/superpowers/specs/2026-09-10-impel-down-design.md`

## Global Constraints

- Python 코드는 **3.9 문법**으로 쓴다 (`match` 문·`X | Y` 타입 표기 금지, `typing.Optional`·`List`·`Dict` 사용). 단위 테스트는 `/usr/bin/python3`(3.9)로 돌고, 실행은 mitmproxy 실행 파일에 내장된 Python으로 돈다.
- `impel/` 모듈은 **mitmproxy를 import하지 않는다.** mitmproxy API는 `addon.py`에서만.
- 테스트는 `/etc/hosts`·`networksetup`·시스템 키체인을 **절대 건드리지 않는다.** 파일 경로와 명령 실행기는 인자로 주입한다.
- repo에 실제 사이트 이름·사용자 경로를 넣지 않는다. 픽스처는 `community.example`·`video.example` 같은 자리표시자만.
- 안내 페이지 멘트는 정확히 `정신 차리고 중요한 일에 집중하세요.`
- 프록시 포트 기본값 `8899`. 설치 경로 `/Library/Application Support/Impel Down/`. plist 라벨 `com.impel-down.engine`. 규칙 파일 `~/Library/Application Support/Impel Down/rules.json`.
- hosts 블록 표식은 `# impel-down` … `# /impel-down`.
- mitmproxy 12.2.3, macOS arm64: `https://downloads.mitmproxy.org/12.2.3/mitmproxy-12.2.3-macos-arm64.tar.gz` (HEAD 요청으로 존재 확인).
- 커밋 메시지 끝에 실행하는 모델의 `Co-Authored-By:` 서명.
- mitmproxy tarball의 sha256은 S0(Task 0 Step 1)에서 재서 `install.sh`의 `MITM_SHA256`에 박는다. 비어 있으면 install.sh가 거부한다.

## Stage = PR 하나

task는 PR로 너무 잘다. 아래 stage 하나가 PR 하나다. stage마다 `git fetch origin` 후 `origin/main`에서 브랜치를 새로 따고, 끝나면 PR → 리뷰 → 머지 → 다음 stage.

| Stage | Task | 브랜치 | PR 제목 |
| --- | --- | --- | --- |
| S0 | 0 | `feat/engine-spike` | 엔진 spike — mitmproxy 기동·연결별 통과·프록시 실측 |
| S1 | 1–5 | `feat/engine-rules` | 엔진 1/3 — 주소 매칭·시간표 판정·규칙 파일·계약 픽스처 |
| S2 | 6–8 | `feat/engine-modules` | 엔진 2/3 — 안내 페이지·hosts 블록·시스템 프록시 유지 |
| S3 | 9–11 | `feat/engine-addon` | 엔진 3/3 — 재확인 루프·mitmproxy 애드온·통합 테스트·설치 스크립트 |

- S0 결과가 spec을 바꾸면 그 spec 수정도 S0 PR에 같이 넣는다.
- S3는 실기 설치(Task 11 Step 5)까지 통과해야 머지.
- PR 생성·머지·push는 건마다 보스 지시로 한다.

## 파일 구조

```
engine/
  addon.py                     mitmproxy 진입점. 훅 3개를 impel/ 모듈에 연결
  impel/__init__.py
  impel/rules.py               주소 정리·매칭, 시간표 판정, 규칙 파일 로드·재로드
  impel/notice.py              안내 페이지 HTML
  impel/hosts.py               /etc/hosts 전용 블록 생성·교체
  impel/proxyconf.py           networksetup 출력 파싱, 프록시 설정 유지
  impel/keeper.py              hosts 미러·프록시 재확인 루프 (tick 단위, 쓰기·설정 함수 주입)
  conftest.py                  (빈 파일) pytest가 engine/를 import 경로에 넣게
  pytest.ini
  tests/test_rules.py
  tests/test_notice.py
  tests/test_hosts.py
  tests/test_proxyconf.py
  tests/test_keeper.py
  tests/test_contract.py       docs/fixtures/ 계약 픽스처 검증
  tests/test_integration.py    mitmdump 실행 파일이 있을 때만 (env IMPEL_MITMDUMP)
  com.impel-down.engine.plist  LaunchDaemon 템플릿 (__RULES__ 치환)
  install.sh
  uninstall.sh
docs/fixtures/rules.json       계약 견본 규칙
docs/fixtures/expectations.json 계약 기대값 표
docs/spikes/2026-09-engine-spike.md  Task 0 측정 결과
```

---

## S0 — spike (`feat/engine-spike`)

### Task 0: 구현 전 실측 (spike)

spec 「구현 전 실측」의 일곱 항목을 실제 기기에서 잰다. 결과가 spec과 다르면 **여기서 멈추고 spec을 고친 뒤** 다음 task로 간다.
브라우저·sudo가 드는 항목은 보스가 실행하고 결과를 불러준다.

**Files:**
- Create: `docs/spikes/2026-09-engine-spike.md`

- [ ] **Step 1: mitmproxy 실행 파일 받기·기동 확인**

```bash
S=/tmp/impel-spike && mkdir -p $S && cd $S
curl -fsSL -o mitm.tar.gz https://downloads.mitmproxy.org/12.2.3/mitmproxy-12.2.3-macos-arm64.tar.gz
shasum -a 256 mitm.tar.gz            # 이 값을 기록 → Task 11 install.sh의 MITM_SHA256
tar -tzf mitm.tar.gz | head          # 안에 뭐가 있는지 — mitmdump 경로를 기록
tar -xzf mitm.tar.gz
MITMDUMP=$(find $S -type f -name mitmdump | head -1); echo "$MITMDUMP"
"$MITMDUMP" --version
```

기록: tarball 안 mitmdump 경로(최상위인지 폴더 안인지), `--version` 출력, 기동이 막히면(서명·격리 경고) 그 메시지.

- [ ] **Step 2: 연결별 복호화/통과 선택 (`tls_clienthello`) 확인**

`/tmp/impel-spike/spike_addon.py`:

```python
from mitmproxy import http, tls

INTERCEPT = {"example.com"}

def tls_clienthello(data: tls.ClientHelloData):
    sni = data.context.client.sni or ""
    if not (sni == "example.com" or sni.endswith(".example.com")):
        data.ignore_connection = True

def request(flow: http.HTTPFlow):
    if flow.request.pretty_host.endswith("example.com"):
        flow.response = http.Response.make(403, "<h1>blocked</h1>", {"Content-Type": "text/html"})
```

```bash
"$MITMDUMP" --listen-host 127.0.0.1 --listen-port 18899 --set confdir=/tmp/impel-spike/conf --set connection_strategy=lazy -s /tmp/impel-spike/spike_addon.py &
sleep 3
# 규칙 밖 호스트: 통과 (인증서를 안 줘도 200 — 복호화 안 했다는 뜻)
curl -s -o /dev/null -w "%{http_code}\n" -x 127.0.0.1:18899 https://www.apple.com/
# 규칙 호스트: 복호화 후 403
curl -s -w "\n%{http_code}\n" -x 127.0.0.1:18899 --cacert /tmp/impel-spike/conf/mitmproxy-ca-cert.pem https://example.com/x
```

기대: 첫 curl `200` (인증서 지정 없이도 성공 = 터널 통과), 둘째 curl 본문 `blocked` + `403`. 다르면 기록.
둘째가 403 대신 200이면 SNI(TLS 연결이 가려는 호스트명) 속성 표기가 빗나간 것이다. `data.context.client.sni`를 `data.client_hello.sni`로 바꿔 한 번 더 돌리고, 통한 표기를 기록한다. Task 10 애드온이 그 표기를 쓴다.

- [ ] **Step 3: 엔진이 죽고 프록시 설정만 남았을 때 트래픽 (보스)**

시스템 프록시를 테스트 포트로 잡는다. 이때 관리자 인증 창이 뜨는지도 기록한다 (spec 「구현 전 실측」의 인증 창 항목). 그다음 mitmdump를 죽이고 브라우저와 curl로 접속해본다.

```bash
# 활성 서비스 전부에 잡히는지도 같이 본다
networksetup -listallnetworkservices | tail -n +2 | grep -v '^\*' | while IFS= read -r svc; do
  networksetup -setwebproxy "$svc" 127.0.0.1 18899; networksetup -setsecurewebproxy "$svc" 127.0.0.1 18899
  echo "== $svc"; networksetup -getsecurewebproxy "$svc" | head -3
done
kill %1   # 위에서 띄운 mitmdump
# Safari·Chrome으로 아무 사이트 → 결과 기록 (전부 실패? 우회해서 열림?)
python3 -c "import urllib.request; print(urllib.request.urlopen('https://www.apple.com/').status)"   # 브라우저 아닌 앱이 시스템 프록시를 타는지
networksetup -listallnetworkservices | tail -n +2 | grep -v '^\*' | while IFS= read -r svc; do
  networksetup -setwebproxystate "$svc" off; networksetup -setsecurewebproxystate "$svc" off
done
```

- [ ] **Step 4: Safari 경유·Private Relay·Chrome QUIC (보스)**

mitmdump를 다시 띄우고 시스템 프록시를 잡은 상태에서:
- Safari로 `https://example.com/x` → `blocked` 페이지가 뜨는지. iCloud Private Relay를 켠 상태와 끈 상태 각각.
- Chrome으로 `https://www.youtube.com/` 접속 후 mitmdump 터미널에 `www.youtube.com` 연결 로그가 찍히는지 (찍히면 QUIC로 우회하지 않은 것).
- 끝나면 프록시 해제, mitmdump 종료.

- [ ] **Step 5: 결과 기록**

`docs/spikes/2026-09-engine-spike.md`에 항목별 결과를 적는다. 형식:

```markdown
# 엔진 spike 결과 (날짜, macOS 버전·아키텍처, mitmproxy 12.2.3)

- tarball sha256: (값)
- tarball 구조: (mitmdump 경로)
- 기동: (정상 / 막힘 + 메시지)
- 연결별 통과·복호화: (200 / 403 blocked — 기대대로 / 다름: …) · SNI 속성 표기: (`data.context.client.sni` / `data.client_hello.sni`)
- 프록시 적용: 활성 서비스 (전부 잡힘 / 안 잡힌 것: …)
- 엔진 죽은 상태 트래픽: 브라우저 (…) · python urllib (…)
- 프록시 설정 변경 시 관리자 인증: (떴다 / 안 떴다)
- Safari 경유: Private Relay 켬 (…) · 끔 (…)
- Chrome QUIC: (프록시 경유 확인 / 우회)
- spec 수정 필요: (없음 / 항목)
```

- [ ] **Step 6: 커밋**

```bash
git add docs/spikes/2026-09-engine-spike.md
git commit -m "docs(spike): 엔진 실측 결과"
```

---

## S1 — 규칙 모듈 (`feat/engine-rules`)

### Task 1: 주소 정리·매칭 (`impel/rules.py` 1/3) + 패키지 골격

**Files:**
- Create: `engine/impel/__init__.py` (빈 파일), `engine/conftest.py` (빈 파일), `engine/pytest.ini`
- Create: `engine/impel/rules.py`
- Test: `engine/tests/test_rules.py`

**Interfaces:**
- Produces: `normalize_address(text: str) -> str`, `is_valid_hostname(host: str) -> bool`, `class Address(host: str, path: Optional[str])` with `Address.parse(text) -> Address`, `.matches_host(host) -> bool`, `.matches(host, path) -> bool`.

- [ ] **Step 1: pytest 설치·골격**

```bash
python3 -m pip install --user pytest
mkdir -p engine/impel engine/tests   # repo 루트에서
cd engine
: > impel/__init__.py; : > conftest.py
printf '[pytest]\ntestpaths = tests\n' > pytest.ini
python3 -m pytest --version
```

- [ ] **Step 2: 실패하는 테스트**

`engine/tests/test_rules.py`:

```python
import pytest
from impel.rules import Address, normalize_address, is_valid_hostname


@pytest.mark.parametrize("raw, expected", [
    ("community.example", "community.example"),
    ("https://www.community.example/", "community.example"),
    ("http://Community.Example/board/list/?id=abc", "community.example/board/list"),
    ("video.example/shorts/abc123#top", "video.example/shorts/abc123"),
    ("  video.example/shorts/  ", "video.example/shorts"),
    ("www.video.example", "video.example"),
])
def test_normalize_address(raw, expected):
    assert normalize_address(raw) == expected


@pytest.mark.parametrize("host, ok", [
    ("community.example", True),
    ("a.b.community.example", True),
    ("x-y.example", True),
    ("-bad.example", False),
    ("nodot", False),
    ("has space.example", False),
    ("community.example/path", False),
    ("", False),
])
def test_is_valid_hostname(host, ok):
    assert is_valid_hostname(host) is ok


def test_parse_whole_host():
    a = Address.parse("https://www.community.example/")
    assert a.host == "community.example" and a.path is None


def test_parse_with_path():
    a = Address.parse("video.example/shorts/")
    assert a.host == "video.example" and a.path == "/shorts"


def test_parse_rejects_bad_host():
    with pytest.raises(ValueError):
        Address.parse("not a host")


@pytest.mark.parametrize("host, expected", [
    ("community.example", True),
    ("gall.community.example", True),
    ("COMMUNITY.EXAMPLE", True),
    ("notcommunity.example", False),
    ("community.example.evil", False),
])
def test_matches_host(host, expected):
    assert Address.parse("community.example").matches_host(host) is expected


@pytest.mark.parametrize("host, path, expected", [
    ("video.example", "/shorts", True),
    ("video.example", "/shorts/abc", True),
    ("m.video.example", "/shorts?x=1", True),
    ("video.example", "/shortsx", False),
    ("video.example", "/", False),
    ("other.example", "/shorts", False),
])
def test_matches_path(host, path, expected):
    assert Address.parse("video.example/shorts").matches(host, path) is expected


def test_whole_host_matches_any_path():
    assert Address.parse("community.example").matches("community.example", "/anything?q=1") is True
```

- [ ] **Step 3: 실패 확인**

Run: `cd engine && python3 -m pytest tests/test_rules.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'impel.rules'`

- [ ] **Step 4: 구현**

`engine/impel/rules.py`:

```python
"""규칙 파일 로드·검증, 주소 매칭, 시간표 판정. mitmproxy에 의존하지 않는다."""
import re
from dataclasses import dataclass
from typing import Optional

_HOST_RE = re.compile(r"^[a-z0-9]([a-z0-9-]*[a-z0-9])?(\.[a-z0-9]([a-z0-9-]*[a-z0-9])?)+$")
_SCHEME_RE = re.compile(r"^[a-zA-Z][a-zA-Z0-9+.-]*://")


def is_valid_hostname(host: str) -> bool:
    return bool(_HOST_RE.match(host))


def normalize_address(text: str) -> str:
    """붙여넣은 주소를 규칙 주소로 정리한다. 앱의 정리 규칙과 같다.

    scheme · 앞의 www. · 끝의 / · ? 뒤 · # 뒤를 떼고 host+path만 남긴다. host는 소문자.
    """
    s = _SCHEME_RE.sub("", text.strip())
    s = s.split("#", 1)[0].split("?", 1)[0]
    if "/" in s:
        host, rest = s.split("/", 1)
        path = "/" + rest.strip("/")
    else:
        host, path = s, ""
    host = host.lower().rstrip(".")
    if host.startswith("www."):
        host = host[4:]
    if path == "/":
        path = ""
    return host + path


@dataclass(frozen=True)
class Address:
    host: str
    path: Optional[str]  # None = 호스트 통째. 있으면 "/"로 시작, 끝 "/" 없음

    @staticmethod
    def parse(text: str) -> "Address":
        s = normalize_address(text)
        if "/" in s:
            host, rest = s.split("/", 1)
            path = "/" + rest  # type: Optional[str]
        else:
            host, path = s, None
        if not is_valid_hostname(host):
            raise ValueError("invalid host in address: %r" % text)
        return Address(host, path)

    def matches_host(self, host: str) -> bool:
        h = host.lower().rstrip(".")
        return h == self.host or h.endswith("." + self.host)

    def matches(self, host: str, path: str) -> bool:
        if not self.matches_host(host):
            return False
        if self.path is None:
            return True
        p = path.split("?", 1)[0]
        return p == self.path or p.startswith(self.path + "/")
```

- [ ] **Step 5: 통과 확인**

Run: `python3 -m pytest tests/test_rules.py -q`
Expected: 모두 PASS

- [ ] **Step 6: 커밋**

```bash
git add engine/impel engine/conftest.py engine/pytest.ini engine/tests/test_rules.py
git commit -m "feat(engine): 주소 정리·매칭 — 서브도메인 점 경계, 경로 세그먼트 경계, 쿼리 무시"
```

---

### Task 2: 시간표 판정·다음 열림 (`impel/rules.py` 2/3)

**Files:**
- Modify: `engine/impel/rules.py` (끝에 추가)
- Test: `engine/tests/test_rules.py` (끝에 추가)

**Interfaces:**
- Consumes: `Address` (Task 1)
- Produces: `DAY_KEYS = ["mon", …, "sun"]` (index == `datetime.weekday()`), `class Group(id: str, title: str, addresses: List[Address], open_hours: Dict[int, FrozenSet[int]])` with `.is_open(at: datetime) -> bool`, `.always_closed() -> bool`, `.next_open(at: datetime) -> Optional[datetime]`, `.matches(host, path) -> bool`.

- [ ] **Step 1: 실패하는 테스트** (`test_rules.py` 끝에 추가)

```python
from datetime import datetime
from impel.rules import Group, DAY_KEYS


def _group(open_hours):
    return Group("g1", "t", [Address.parse("community.example")], open_hours)


SAT, SUN, MON = 5, 6, 0


def test_day_keys_align_with_weekday():
    assert DAY_KEYS[datetime(2026, 9, 12).weekday()] == "sat"


def test_is_open_inside_window():
    g = _group({SAT: frozenset({20, 21, 22})})
    assert g.is_open(datetime(2026, 9, 12, 20, 0)) is True
    assert g.is_open(datetime(2026, 9, 12, 22, 59)) is True


def test_is_open_outside_window():
    g = _group({SAT: frozenset({20, 21, 22})})
    assert g.is_open(datetime(2026, 9, 12, 19, 59)) is False
    assert g.is_open(datetime(2026, 9, 12, 23, 0)) is False
    assert g.is_open(datetime(2026, 9, 11, 21, 0)) is False  # 금요일


def test_always_closed():
    assert _group({}).always_closed() is True
    assert _group({SAT: frozenset()}).always_closed() is True
    assert _group({SAT: frozenset({1})}).always_closed() is False


def test_next_open_same_day():
    g = _group({SAT: frozenset({20, 21, 22})})
    assert g.next_open(datetime(2026, 9, 12, 19, 30)) == datetime(2026, 9, 12, 20, 0)


def test_next_open_crosses_week():
    g = _group({SAT: frozenset({20})})
    # 토 21:00 → 다음 주 토 20:00
    assert g.next_open(datetime(2026, 9, 12, 21, 0)) == datetime(2026, 9, 19, 20, 0)


def test_next_open_none_when_always_closed():
    assert _group({}).next_open(datetime(2026, 9, 12, 10, 0)) is None


def test_next_open_skips_current_hour():
    # 지금 20:30이고 20시가 열려 있으면 is_open이 True라 next_open은 쓰이지 않지만,
    # 닫힌 시각에서 부르면 현재 시각 칸은 후보가 아니다
    g = _group({SUN: frozenset({0})})
    assert g.next_open(datetime(2026, 9, 12, 23, 30)) == datetime(2026, 9, 13, 0, 0)
```

- [ ] **Step 2: 실패 확인**

Run: `python3 -m pytest tests/test_rules.py -q`
Expected: FAIL — `ImportError: cannot import name 'Group'`

- [ ] **Step 3: 구현** (`rules.py` 끝에 추가. import 줄은 파일 상단에 합친다)

```python
from datetime import datetime, timedelta
from typing import Dict, FrozenSet, List

DAY_KEYS = ["mon", "tue", "wed", "thu", "fri", "sat", "sun"]  # index == datetime.weekday()
_WEEK_HOURS = 24 * 7


@dataclass
class Group:
    id: str
    title: str
    addresses: List[Address]
    open_hours: Dict[int, FrozenSet[int]]  # weekday(0=월) -> 열린 시각(그 시부터 한 시간)

    def is_open(self, at: datetime) -> bool:
        return at.hour in self.open_hours.get(at.weekday(), frozenset())

    def always_closed(self) -> bool:
        return not any(self.open_hours.values())

    def next_open(self, at: datetime) -> Optional[datetime]:
        """지금 이후 첫 열린 칸의 시작 시각. 7일 안에 없으면 None."""
        start = at.replace(minute=0, second=0, microsecond=0)
        for offset in range(1, _WEEK_HOURS + 1):
            t = start + timedelta(hours=offset)
            if t.hour in self.open_hours.get(t.weekday(), frozenset()):
                return t
        return None

    def matches(self, host: str, path: str) -> bool:
        return any(a.matches(host, path) for a in self.addresses)
```

- [ ] **Step 4: 통과 확인**

Run: `python3 -m pytest tests/test_rules.py -q`
Expected: 모두 PASS

- [ ] **Step 5: 커밋**

```bash
git add engine/impel/rules.py engine/tests/test_rules.py
git commit -m "feat(engine): 묶음 시간표 판정과 다음 열림 계산"
```

---

### Task 3: 규칙 파일 파싱·판정·미러 목록 (`impel/rules.py` 3/3)

**Files:**
- Modify: `engine/impel/rules.py` (끝에 추가)
- Test: `engine/tests/test_rules.py` (끝에 추가)

**Interfaces:**
- Consumes: `Address`, `Group`, `DAY_KEYS`
- Produces: `class Rules(groups: List[Group])` with `Rules.from_json(text: str) -> Rules` (스키마 위반 시 `ValueError`), `Rules.empty() -> Rules`, `.intercept_host(host) -> bool`, `.decide(host, path, at) -> Optional[Group]` (차단한 묶음, 없으면 None), `.mirror_hosts() -> List[str]`.

- [ ] **Step 1: 실패하는 테스트** (`test_rules.py` 끝에 추가)

```python
import json
from impel.rules import Rules

SAMPLE = {
    "version": 1,
    "groups": [
        {"id": "g-community", "title": "커뮤니티",
         "addresses": ["community.example", "video.example/shorts"],
         "open": {"sat": [20, 21, 22], "sun": [20, 21, 22]}},
        {"id": "g-always", "title": "상시", "addresses": ["forum.example"], "open": {}},
    ],
}


def test_from_json_parses_groups():
    r = Rules.from_json(json.dumps(SAMPLE))
    assert [g.id for g in r.groups] == ["g-community", "g-always"]
    assert r.groups[0].open_hours == {5: frozenset({20, 21, 22}), 6: frozenset({20, 21, 22})}
    assert r.groups[1].always_closed()


@pytest.mark.parametrize("bad", [
    '{"version": 2, "groups": []}',
    '{"version": 1, "groups": [{"title": "no id", "addresses": []}]}',
    '{"version": 1, "groups": [{"id": "x", "addresses": "notalist"}]}',
    '{"version": 1, "groups": [{"id": "x", "addresses": [], "open": {"funday": [1]}}]}',
    '{"version": 1, "groups": [{"id": "x", "addresses": [], "open": {"mon": [24]}}]}',
    '{"version": 1, "groups": [{"id": "x", "addresses": ["not a host"]}]}',
    '[]',
    'not json',
])
def test_from_json_rejects_bad_schema(bad):
    with pytest.raises(ValueError):
        Rules.from_json(bad)


def test_from_json_skips_blank_addresses():
    r = Rules.from_json('{"version": 1, "groups": [{"id": "x", "addresses": ["", "  ", "a.example"]}]}')
    assert [a.host for a in r.groups[0].addresses] == ["a.example"]


def test_intercept_host():
    r = Rules.from_json(json.dumps(SAMPLE))
    assert r.intercept_host("gall.community.example") is True
    assert r.intercept_host("video.example") is True   # 경로 주소도 호스트는 복호화 대상
    assert r.intercept_host("www.apple.example") is False


def test_decide_blocks_when_closed():
    r = Rules.from_json(json.dumps(SAMPLE))
    g = r.decide("community.example", "/", datetime(2026, 9, 11, 21, 0))  # 금요일
    assert g is not None and g.id == "g-community"


def test_decide_passes_when_open():
    r = Rules.from_json(json.dumps(SAMPLE))
    assert r.decide("community.example", "/", datetime(2026, 9, 12, 21, 0)) is None  # 토요일 21시


def test_decide_passes_unmatched_path():
    r = Rules.from_json(json.dumps(SAMPLE))
    assert r.decide("video.example", "/watch", datetime(2026, 9, 11, 21, 0)) is None


def test_decide_any_closed_group_blocks():
    doc = {"version": 1, "groups": [
        {"id": "open-now", "title": "a", "addresses": ["dup.example"],
         "open": {k: list(range(24)) for k in DAY_KEYS}},
        {"id": "closed", "title": "b", "addresses": ["dup.example"], "open": {}},
    ]}
    g = Rules.from_json(json.dumps(doc)).decide("dup.example", "/", datetime(2026, 9, 11, 21, 0))
    assert g is not None and g.id == "closed"


def test_mirror_hosts_only_always_closed_whole_hosts():
    r = Rules.from_json(json.dumps(SAMPLE))
    assert r.mirror_hosts() == ["forum.example", "www.forum.example"]


def test_empty():
    assert Rules.empty().groups == []
```

- [ ] **Step 2: 실패 확인**

Run: `python3 -m pytest tests/test_rules.py -q`
Expected: FAIL — `ImportError: cannot import name 'Rules'`

- [ ] **Step 3: 구현** (`rules.py` 끝에 추가, `import json` 상단에)

```python
import json


@dataclass
class Rules:
    groups: List[Group]

    @staticmethod
    def empty() -> "Rules":
        return Rules([])

    @staticmethod
    def from_json(text: str) -> "Rules":
        """rules.json 본문을 파싱·검증한다. 어긋나면 ValueError."""
        doc = json.loads(text)  # JSONDecodeError는 ValueError의 하위 클래스
        if not isinstance(doc, dict) or doc.get("version") != 1:
            raise ValueError("unsupported rules document")
        raw_groups = doc.get("groups", [])
        if not isinstance(raw_groups, list):
            raise ValueError("groups must be a list")
        groups = []
        for g in raw_groups:
            if not isinstance(g, dict):
                raise ValueError("group must be an object")
            gid = g.get("id")
            if not isinstance(gid, str) or not gid:
                raise ValueError("group id required")
            title = g.get("title", "")
            if not isinstance(title, str):
                raise ValueError("title must be a string")
            raw_addrs = g.get("addresses", [])
            if not isinstance(raw_addrs, list):
                raise ValueError("addresses must be a list")
            addresses = [Address.parse(a) for a in raw_addrs if isinstance(a, str) and a.strip()]
            raw_open = g.get("open") or {}
            if not isinstance(raw_open, dict):
                raise ValueError("open must be an object")
            open_hours = {}  # type: Dict[int, FrozenSet[int]]
            for key, hours in raw_open.items():
                if key not in DAY_KEYS:
                    raise ValueError("unknown day key: %r" % key)
                if not isinstance(hours, list) or not all(
                    isinstance(h, int) and not isinstance(h, bool) and 0 <= h <= 23 for h in hours
                ):
                    raise ValueError("hours for %s must be integers 0..23" % key)
                open_hours[DAY_KEYS.index(key)] = frozenset(hours)
            groups.append(Group(gid, title, addresses, open_hours))
        return Rules(groups)

    def intercept_host(self, host: str) -> bool:
        return any(a.matches_host(host) for g in self.groups for a in g.addresses)

    def decide(self, host: str, path: str, at: datetime) -> Optional[Group]:
        """차단해야 하면 그 묶음(여럿이면 첫째), 아니면 None."""
        for g in self.groups:
            if g.matches(host, path) and not g.is_open(at):
                return g
        return None

    def mirror_hosts(self) -> List[str]:
        """hosts 블록에 미러할 호스트: 24시간 차단 묶음의 호스트 통째 주소 + www. 변형."""
        out = set()
        for g in self.groups:
            if not g.always_closed():
                continue
            for a in g.addresses:
                if a.path is None:
                    out.add(a.host)
                    out.add("www." + a.host)
        return sorted(out)
```

- [ ] **Step 4: 통과 확인**

Run: `python3 -m pytest tests/test_rules.py -q`
Expected: 모두 PASS

- [ ] **Step 5: 커밋**

```bash
git add engine/impel/rules.py engine/tests/test_rules.py
git commit -m "feat(engine): 규칙 파일 파싱·검증, 차단 판정, hosts 미러 목록"
```

---

### Task 4: 규칙 파일 재로드 (`RulesFile`)

**Files:**
- Modify: `engine/impel/rules.py` (끝에 추가)
- Test: `engine/tests/test_rules.py` (끝에 추가)

**Interfaces:**
- Produces: `class RulesFile(path: str, log: Callable[[str], None] = None)` with `.current() -> Rules` (수정 시각이 바뀌었을 때만 다시 읽음, 실패 시 직전 규칙 유지), `.version: int` (성공적으로 읽을 때마다 +1).

- [ ] **Step 1: 실패하는 테스트**

```python
import os
import time
from impel.rules import RulesFile

GOOD = '{"version": 1, "groups": [{"id": "a", "title": "a", "addresses": ["a.example"]}]}'
GOOD2 = '{"version": 1, "groups": [{"id": "b", "title": "b", "addresses": ["b.example"]}]}'


def _write(path, text):
    path.write_text(text, encoding="utf-8")
    # mtime 해상도보다 확실히 뒤로
    t = time.time() + 2
    os.utime(str(path), (t, t))


def test_rulesfile_missing_is_empty(tmp_path):
    rf = RulesFile(str(tmp_path / "rules.json"))
    assert rf.current().groups == [] and rf.version == 0


def test_rulesfile_loads_and_reloads_on_mtime(tmp_path):
    p = tmp_path / "rules.json"
    _write(p, GOOD)
    rf = RulesFile(str(p))
    assert rf.current().groups[0].id == "a" and rf.version == 1
    assert rf.current().groups[0].id == "a" and rf.version == 1  # mtime 같으면 안 읽음
    _write(p, GOOD2)
    assert rf.current().groups[0].id == "b" and rf.version == 2


def test_rulesfile_keeps_last_good_on_bad_content(tmp_path):
    p = tmp_path / "rules.json"
    _write(p, GOOD)
    logs = []
    rf = RulesFile(str(p), log=logs.append)
    rf.current()
    _write(p, "{ broken")
    assert rf.current().groups[0].id == "a"
    assert rf.version == 1 and logs and "reload failed" in logs[0]
```

- [ ] **Step 2: 실패 확인**

Run: `python3 -m pytest tests/test_rules.py -q`
Expected: FAIL — `ImportError: cannot import name 'RulesFile'`

- [ ] **Step 3: 구현** (`rules.py` 끝에 추가. `import os`, `from typing import Callable` 상단에)

```python
import os
from typing import Callable


class RulesFile:
    """규칙 파일을 수정 시각 기준으로 다시 읽는다. 실패하면 직전 규칙을 유지한다."""

    def __init__(self, path: str, log: Optional[Callable[[str], None]] = None):
        self.path = path
        self.version = 0
        self._mtime = None  # type: Optional[int]
        self._rules = Rules.empty()
        self._log = log or (lambda msg: None)

    def current(self) -> Rules:
        try:
            mtime = os.stat(self.path).st_mtime_ns
        except FileNotFoundError:
            return self._rules
        if mtime == self._mtime:
            return self._rules
        self._mtime = mtime
        try:
            with open(self.path, encoding="utf-8") as f:
                text = f.read()
            self._rules = Rules.from_json(text)
            self.version += 1
        except (OSError, ValueError) as e:
            self._log("rules reload failed: %s" % e)
        return self._rules
```

- [ ] **Step 4: 통과 확인**

Run: `python3 -m pytest tests/test_rules.py -q`
Expected: 모두 PASS

- [ ] **Step 5: 커밋**

```bash
git add engine/impel/rules.py engine/tests/test_rules.py
git commit -m "feat(engine): 규칙 파일 수정 시각 기준 재로드, 실패 시 직전 규칙 유지"
```

---

### Task 5: 계약 픽스처 (`docs/fixtures/`) + 계약 테스트

앱(Swift)과 엔진(Python)이 같은 파일·같은 표로 같은 답을 내게 하는 픽스처. 앱 plan의 테스트도 이 두 파일을 읽는다.

**Files:**
- Create: `docs/fixtures/rules.json`, `docs/fixtures/expectations.json`
- Test: `engine/tests/test_contract.py`

**Interfaces:**
- Produces: `expectations.json` 형식.
	- `cases[]` 항목: `{group, at (ISO 로컬, 초 없음), closed (bool), next_open (ISO 또는 null; closed일 때만 값), summary (문자열 배열, 월요일 줄부터)}`
	- `decide[]` 항목: `{host, path, at, blocked_by (묶음 id 또는 null)}`
	- `summary`는 앱만, `decide`는 엔진만 검증한다.
- `rules.json`의 `video.example/shorts`(g-community)와 `video.example`(g-always)는 일부러 겹친다. "하나라도 닫혀 있으면 차단"을 `decide`가 잠근다.
	- 카드 상태는 묶음 기준이라 g-community 카드가 "열림"일 때도 `video.example/shorts`는 막힌다. 이 점은 앱 plan에 그대로 넘긴다.

- [ ] **Step 1: 픽스처 작성**

`docs/fixtures/rules.json`:

```json
{
  "version": 1,
  "groups": [
    {
      "id": "g-community",
      "title": "커뮤니티",
      "addresses": ["community.example", "forum.example", "video.example/shorts"],
      "open": { "sat": [20, 21, 22], "sun": [20, 21, 22] }
    },
    {
      "id": "g-always",
      "title": "상시",
      "addresses": ["video.example"],
      "open": {}
    },
    {
      "id": "g-split",
      "title": "점심·저녁",
      "addresses": ["news.example"],
      "open": { "mon": [9, 10, 14, 15], "tue": [9, 10, 14, 15] }
    },
    {
      "id": "g-midnight",
      "title": "자정 넘김",
      "addresses": ["late.example"],
      "open": { "sun": [23], "mon": [0] }
    }
  ]
}
```

`docs/fixtures/expectations.json` (2026-09-12 토 · 09-13 일 · 09-14 월):

```json
{
  "cases": [
    { "group": "g-community", "at": "2026-09-12T20:30", "closed": false, "next_open": null, "summary": ["토·일 20–23"] },
    { "group": "g-community", "at": "2026-09-12T19:30", "closed": true,  "next_open": "2026-09-12T20:00", "summary": ["토·일 20–23"] },
    { "group": "g-community", "at": "2026-09-13T23:30", "closed": true,  "next_open": "2026-09-19T20:00", "summary": ["토·일 20–23"] },
    { "group": "g-always",    "at": "2026-09-12T20:30", "closed": true,  "next_open": null, "summary": ["상시"] },
    { "group": "g-split",     "at": "2026-09-14T11:00", "closed": true,  "next_open": "2026-09-14T14:00", "summary": ["월·화 9–11 · 14–16"] },
    { "group": "g-split",     "at": "2026-09-14T15:59", "closed": false, "next_open": null, "summary": ["월·화 9–11 · 14–16"] },
    { "group": "g-midnight",  "at": "2026-09-13T23:30", "closed": false, "next_open": null, "summary": ["월 0–1", "일 23–24"] },
    { "group": "g-midnight",  "at": "2026-09-14T01:00", "closed": true,  "next_open": "2026-09-20T23:00", "summary": ["월 0–1", "일 23–24"] }
  ],
  "decide": [
    { "host": "gall.community.example", "path": "/board?id=1", "at": "2026-09-11T21:00", "blocked_by": "g-community" },
    { "host": "community.example",      "path": "/",           "at": "2026-09-12T21:00", "blocked_by": null },
    { "host": "video.example",          "path": "/shorts/abc", "at": "2026-09-12T21:00", "blocked_by": "g-always" },
    { "host": "video.example",          "path": "/watch",      "at": "2026-09-12T21:00", "blocked_by": "g-always" },
    { "host": "news.example",           "path": "/",           "at": "2026-09-14T14:30", "blocked_by": null },
    { "host": "news.example",           "path": "/",           "at": "2026-09-14T13:30", "blocked_by": "g-split" },
    { "host": "unlisted.example",       "path": "/",           "at": "2026-09-14T13:30", "blocked_by": null }
  ]
}
```

`video.example`의 두 줄: 토 21:00에 g-community는 열려 있지만 g-always가 닫혀 있어 `/shorts/abc`도 막힌다. 여러 묶음에 걸리면 닫힌 첫 묶음이 답이다.

- [ ] **Step 2: 계약 테스트**

`engine/tests/test_contract.py`:

```python
import json
import os
from datetime import datetime

from impel.rules import Rules

FIXTURES = os.path.join(os.path.dirname(__file__), "..", "..", "docs", "fixtures")


def _load(name):
    with open(os.path.join(FIXTURES, name), encoding="utf-8") as f:
        return f.read()


def test_contract_cases():
    rules = Rules.from_json(_load("rules.json"))
    by_id = {g.id: g for g in rules.groups}
    cases = json.loads(_load("expectations.json"))["cases"]
    assert cases, "expectations.json has no cases"
    for c in cases:
        g = by_id[c["group"]]
        at = datetime.fromisoformat(c["at"])
        closed = not g.is_open(at)
        assert closed == c["closed"], c
        expected_next = datetime.fromisoformat(c["next_open"]) if c["next_open"] else None
        actual_next = g.next_open(at) if closed else None
        assert actual_next == expected_next, c


def test_contract_decide():
    rules = Rules.from_json(_load("rules.json"))
    cases = json.loads(_load("expectations.json"))["decide"]
    assert cases, "expectations.json has no decide cases"
    for c in cases:
        g = rules.decide(c["host"], c["path"], datetime.fromisoformat(c["at"]))
        assert (g.id if g else None) == c["blocked_by"], c
```

- [ ] **Step 3: 통과 확인**

Run: `python3 -m pytest tests/test_contract.py -q`
Expected: PASS
실패하면 기대값 표가 아니라 Task 2·3 구현을 먼저 의심한다. 표를 손보려면 spec 「메인 창」 구간 규칙과 대조한다.

- [ ] **Step 4: 커밋**

```bash
git add docs/fixtures engine/tests/test_contract.py
git commit -m "test(contract): 앱·엔진 공용 규칙 견본과 기대값 표"
```

---

## S2 — 부속 모듈 (`feat/engine-modules`)

### Task 6: 안내 페이지 (`impel/notice.py`)

**Files:**
- Create: `engine/impel/notice.py`
- Test: `engine/tests/test_notice.py`

**Interfaces:**
- Produces: `MESSAGE: str`, `format_next_open(t: datetime) -> str` (예: `"토 20:00"`), `render(title: str, next_open: Optional[datetime], host: str) -> str` (완전한 HTML 문서).

- [ ] **Step 1: 실패하는 테스트**

```python
from datetime import datetime
from impel.notice import MESSAGE, format_next_open, render


def test_message_verbatim():
    assert MESSAGE == "정신 차리고 중요한 일에 집중하세요."


def test_format_next_open():
    assert format_next_open(datetime(2026, 9, 12, 20, 0)) == "토 20:00"
    assert format_next_open(datetime(2026, 9, 14, 0, 0)) == "월 00:00"


def test_render_with_next_open():
    html = render("커뮤니티", datetime(2026, 9, 12, 20, 0), "gall.community.example")
    assert html.startswith("<!doctype html>")
    assert MESSAGE in html and "커뮤니티" in html
    assert "다음 열림 토 20:00" in html
    assert "gall.community.example" in html


def test_render_without_next_open():
    html = render("상시", None, "video.example")
    assert "다음 열림" not in html


def test_render_escapes_html():
    html = render("<b>x</b>", None, "evil.example/<script>")
    assert "<b>x</b>" not in html and "&lt;b&gt;x&lt;/b&gt;" in html
    assert "<script>" not in html
```

- [ ] **Step 2: 실패 확인**

Run: `python3 -m pytest tests/test_notice.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'impel.notice'`

- [ ] **Step 3: 구현**

`engine/impel/notice.py`:

```python
"""차단 시 브라우저에 돌려주는 안내 페이지."""
import html
from datetime import datetime
from typing import Optional

MESSAGE = "정신 차리고 중요한 일에 집중하세요."
_DAYS_KO = ["월", "화", "수", "목", "금", "토", "일"]

_TEMPLATE = """<!doctype html>
<html lang="ko">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Impel Down</title>
<style>
  html, body { height: 100%; margin: 0; }
  body { display: flex; align-items: center; justify-content: center; background: #111; color: #eee;
         font-family: -apple-system, "Apple SD Gothic Neo", sans-serif; text-align: center; }
  main { padding: 32px; }
  .msg { font-size: 20px; font-weight: 600; margin-bottom: 18px; }
  .title { font-size: 15px; opacity: .85; }
  .next { font-size: 14px; opacity: .7; margin-top: 6px; }
  .host { font-size: 12px; opacity: .45; margin-top: 22px; font-family: ui-monospace, Menlo, monospace; }
</style>
</head>
<body>
<main>
{{body}}
</main>
</body>
</html>
"""


def format_next_open(t: datetime) -> str:
    return "%s %02d:00" % (_DAYS_KO[t.weekday()], t.hour)


def render(title: str, next_open: Optional[datetime], host: str) -> str:
    lines = [
        '<div class="msg">%s</div>' % html.escape(MESSAGE),
        '<div class="title">%s</div>' % html.escape(title),
    ]
    if next_open is not None:
        lines.append('<div class="next">다음 열림 %s</div>' % html.escape(format_next_open(next_open)))
    lines.append('<div class="host">%s</div>' % html.escape(host))
    return _TEMPLATE.replace("{{body}}", "\n".join(lines))
```

- [ ] **Step 4: 통과 확인**

Run: `python3 -m pytest tests/test_notice.py -q`
Expected: 모두 PASS

- [ ] **Step 5: 커밋**

```bash
git add engine/impel/notice.py engine/tests/test_notice.py
git commit -m "feat(engine): 차단 안내 페이지 HTML"
```

---

### Task 7: hosts 전용 블록 (`impel/hosts.py`)

**Files:**
- Create: `engine/impel/hosts.py`
- Test: `engine/tests/test_hosts.py`

**Interfaces:**
- Consumes: `is_valid_hostname` (Task 1)
- Produces: `BEGIN = "# impel-down"`, `END = "# /impel-down"`, `render_block(hosts: List[str]) -> str`, `replace_block(text: str, hosts: List[str]) -> str`, `write_hosts(hosts: List[str], path: str = "/etc/hosts", flush: Optional[Callable[[], None]] = None) -> bool` (내용이 바뀌었으면 True), `flush_dns() -> None`.

- [ ] **Step 1: 실패하는 테스트**

```python
from impel.hosts import BEGIN, END, render_block, replace_block, write_hosts

BASE = "127.0.0.1\tlocalhost\n::1 localhost\n"


def test_render_block_sorted_and_validated():
    out = render_block(["b.example", "a.example", "bad host", "x.example/path"])
    assert out == "# impel-down\n127.0.0.1 a.example\n127.0.0.1 b.example\n# /impel-down\n"


def test_render_block_empty():
    assert render_block([]) == ""
    assert render_block(["not valid"]) == ""


def test_replace_block_appends_when_absent():
    out = replace_block(BASE, ["a.example"])
    assert out == BASE + "\n# impel-down\n127.0.0.1 a.example\n# /impel-down\n"


def test_replace_block_replaces_existing_and_keeps_rest():
    old = BASE + "\n# impel-down\n127.0.0.1 old.example\n# /impel-down\n\n# other block\n1.2.3.4 keep.example\n"
    out = replace_block(old, ["new.example"])
    assert "old.example" not in out
    assert "1.2.3.4 keep.example" in out
    assert out.endswith("# impel-down\n127.0.0.1 new.example\n# /impel-down\n")
    assert out.count(BEGIN) == 1 and out.count(END) == 1


def test_replace_block_removes_when_empty():
    old = BASE + "\n# impel-down\n127.0.0.1 old.example\n# /impel-down\n"
    assert replace_block(old, []) == BASE


def test_replace_block_idempotent():
    once = replace_block(BASE, ["a.example"])
    assert replace_block(once, ["a.example"]) == once


def test_write_hosts_atomic_and_flushes(tmp_path):
    p = tmp_path / "hosts"
    p.write_text(BASE, encoding="utf-8")
    flushed = []
    assert write_hosts(["a.example"], path=str(p), flush=lambda: flushed.append(1)) is True
    assert p.read_text(encoding="utf-8").endswith("127.0.0.1 a.example\n# /impel-down\n")
    assert flushed == [1]
    assert not (tmp_path / "hosts.impel-tmp").exists()


def test_write_hosts_noop_when_unchanged(tmp_path):
    p = tmp_path / "hosts"
    p.write_text(BASE, encoding="utf-8")
    write_hosts(["a.example"], path=str(p), flush=lambda: None)
    flushed = []
    assert write_hosts(["a.example"], path=str(p), flush=lambda: flushed.append(1)) is False
    assert flushed == []
```

- [ ] **Step 2: 실패 확인**

Run: `python3 -m pytest tests/test_hosts.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'impel.hosts'`

- [ ] **Step 3: 구현**

`engine/impel/hosts.py`:

```python
"""/etc/hosts의 전용 블록을 규칙에 맞춰 다시 쓴다. 블록 밖은 건드리지 않는다."""
import os
import subprocess
from typing import Callable, List, Optional

from .rules import is_valid_hostname

BEGIN = "# impel-down"
END = "# /impel-down"


def render_block(hosts: List[str]) -> str:
    valid = sorted({h for h in hosts if is_valid_hostname(h)})
    if not valid:
        return ""
    return "\n".join([BEGIN] + ["127.0.0.1 %s" % h for h in valid] + [END]) + "\n"


def replace_block(text: str, hosts: List[str]) -> str:
    kept = []
    skipping = False
    for line in text.splitlines():
        if line.strip() == BEGIN:
            skipping = True
            continue
        if line.strip() == END:
            skipping = False
            continue
        if not skipping:
            kept.append(line)
    body = "\n".join(kept).rstrip("\n") + "\n"
    block = render_block(hosts)
    if not block:
        return body
    return body + "\n" + block


def flush_dns() -> None:
    subprocess.run(["dscacheutil", "-flushcache"], check=False)
    subprocess.run(["killall", "-HUP", "mDNSResponder"], check=False)


def write_hosts(hosts: List[str], path: str = "/etc/hosts",
                flush: Optional[Callable[[], None]] = None) -> bool:
    """블록을 갱신한다. 내용이 바뀌었으면 True. 쓰기는 임시 파일 후 교체."""
    with open(path, encoding="utf-8") as f:
        old = f.read()
    new = replace_block(old, hosts)
    if new == old:
        return False
    tmp = path + ".impel-tmp"
    with open(tmp, "w", encoding="utf-8") as f:
        f.write(new)
    os.chmod(tmp, 0o644)
    os.replace(tmp, path)
    (flush or flush_dns)()
    return True
```

- [ ] **Step 4: 통과 확인**

Run: `python3 -m pytest tests/test_hosts.py -q`
Expected: 모두 PASS

- [ ] **Step 5: 커밋**

```bash
git add engine/impel/hosts.py engine/tests/test_hosts.py
git commit -m "feat(engine): hosts 전용 블록 생성·교체, 검증 통과 호스트만"
```

---

### Task 8: 시스템 프록시 설정 유지 (`impel/proxyconf.py`)

파서는 macOS `networksetup`의 실제 출력 형식을 기준으로 한다. Task 안 픽스처는 그 형식에 비활성 서비스 한 줄(`*Thunderbolt Bridge`)을 더한 것이다.

**Files:**
- Create: `engine/impel/proxyconf.py`
- Test: `engine/tests/test_proxyconf.py`

**Interfaces:**
- Produces: `Runner = Callable[[List[str]], str]` (명령 인자 목록 → stdout), `run(cmd) -> str`, `parse_services(text) -> List[str]` (활성 서비스만), `parse_proxy(text) -> Tuple[bool, str, int]`, `ensure_all(host, port, runner=run) -> Tuple[List[str], List[str]]` (setter 뒤 getter로 재확인한 (실제 바뀐 항목, 실패한 항목)), `clear_all(runner=run) -> None`.

- [ ] **Step 1: 실패하는 테스트**

```python
from impel.proxyconf import parse_services, parse_proxy, ensure_all, clear_all

SERVICES = """An asterisk (*) denotes that a network service is disabled.
USB 10/100/1000 LAN
*Thunderbolt Bridge
Wi-Fi
iPhone USB
"""

OFF = """Enabled: No
Server: 
Port: 0
Authenticated Proxy Enabled: 0
"""

ON = """Enabled: Yes
Server: 127.0.0.1
Port: 8899
Authenticated Proxy Enabled: 0
"""


def test_parse_services_skips_header_and_disabled():
    assert parse_services(SERVICES) == ["USB 10/100/1000 LAN", "Wi-Fi", "iPhone USB"]


def test_parse_proxy():
    assert parse_proxy(OFF) == (False, "", 0)
    assert parse_proxy(ON) == (True, "127.0.0.1", 8899)


class FakeRunner:
    def __init__(self, state, deny=()):
        self.state = state  # (service, kind) -> (enabled, server, port)
        self.deny = set(deny)  # setter를 무시하는 서비스 (설정 거부 흉내)
        self.calls = []

    def __call__(self, cmd):
        self.calls.append(cmd)
        if cmd[1] == "-listallnetworkservices":
            return SERVICES
        kind = "web" if "webproxy" in cmd[1] and "secure" not in cmd[1] else "secure"
        svc = cmd[2]
        if cmd[1] in ("-getwebproxy", "-getsecurewebproxy"):
            en, s, p = self.state.get((svc, kind), (False, "", 0))
            return "Enabled: %s\nServer: %s\nPort: %d\nAuthenticated Proxy Enabled: 0\n" % ("Yes" if en else "No", s, p)
        if cmd[1] in ("-setwebproxy", "-setsecurewebproxy"):
            if svc not in self.deny:
                self.state[(svc, kind)] = (True, cmd[3], int(cmd[4]))
            return ""
        if cmd[1] in ("-setwebproxystate", "-setsecurewebproxystate"):
            en, s, p = self.state.get((svc, kind), (False, "", 0))
            self.state[(svc, kind)] = (cmd[3] == "on", s, p)
            return ""
        raise AssertionError("unexpected command %r" % cmd)


def test_ensure_all_sets_missing_only():
    r = FakeRunner({("Wi-Fi", "web"): (True, "127.0.0.1", 8899)})
    changed, failed = ensure_all("127.0.0.1", 8899, runner=r)
    assert ["networksetup", "-setwebproxy", "Wi-Fi", "127.0.0.1", "8899"] not in r.calls  # 이미 맞던 건 안 건드림
    assert r.state[("Wi-Fi", "secure")] == (True, "127.0.0.1", 8899)
    assert r.state[("USB 10/100/1000 LAN", "web")] == (True, "127.0.0.1", 8899)
    assert ("Thunderbolt Bridge", "web") not in r.state  # 비활성 서비스는 건드리지 않음
    assert len(changed) == 5 and failed == []  # 3 서비스 × 2 - 이미 맞던 1


def test_ensure_all_fixes_wrong_port():
    r = FakeRunner({("Wi-Fi", "web"): (True, "127.0.0.1", 1234)})
    changed, _ = ensure_all("127.0.0.1", 8899, runner=r)
    assert r.state[("Wi-Fi", "web")] == (True, "127.0.0.1", 8899)
    assert "Wi-Fi -setwebproxy" in changed


def test_ensure_all_noop_when_all_set():
    state = {(s, k): (True, "127.0.0.1", 8899)
             for s in ["USB 10/100/1000 LAN", "Wi-Fi", "iPhone USB"] for k in ("web", "secure")}
    r = FakeRunner(state)
    assert ensure_all("127.0.0.1", 8899, runner=r) == ([], [])
    assert not [c for c in r.calls if c[1].startswith("-set")]


def test_ensure_all_reports_failed_service():
    # setter가 먹히지 않은 서비스는 changed가 아니라 failed로
    r = FakeRunner({}, deny={"iPhone USB"})
    changed, failed = ensure_all("127.0.0.1", 8899, runner=r)
    assert failed == ["iPhone USB -setwebproxy", "iPhone USB -setsecurewebproxy"]
    assert not [c for c in changed if c.startswith("iPhone USB")]
    assert len(changed) == 4


def test_clear_all_turns_off_every_active_service():
    state = {("Wi-Fi", "web"): (True, "127.0.0.1", 8899), ("Wi-Fi", "secure"): (True, "127.0.0.1", 8899)}
    r = FakeRunner(state)
    clear_all(runner=r)
    assert r.state[("Wi-Fi", "web")][0] is False and r.state[("Wi-Fi", "secure")][0] is False
    assert [c[2] for c in r.calls if c[1] == "-setwebproxystate"] == ["USB 10/100/1000 LAN", "Wi-Fi", "iPhone USB"]
```

- [ ] **Step 2: 실패 확인**

Run: `python3 -m pytest tests/test_proxyconf.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'impel.proxyconf'`

- [ ] **Step 3: 구현**

`engine/impel/proxyconf.py`:

```python
"""macOS 시스템 프록시(HTTP·HTTPS)를 networksetup으로 읽고 유지한다."""
import subprocess
from typing import Callable, List, Tuple

Runner = Callable[[List[str]], str]

_PAIRS = (("-getwebproxy", "-setwebproxy", "-setwebproxystate"),
          ("-getsecurewebproxy", "-setsecurewebproxy", "-setsecurewebproxystate"))


def run(cmd: List[str]) -> str:
    return subprocess.run(cmd, capture_output=True, text=True, check=False).stdout


def parse_services(text: str) -> List[str]:
    """`networksetup -listallnetworkservices` 출력에서 활성 서비스 이름만.
    첫 줄은 안내문, `*`로 시작하면 비활성."""
    names = []
    for line in text.splitlines()[1:]:
        s = line.strip()
        if s and not s.startswith("*"):
            names.append(s)
    return names


def parse_proxy(text: str) -> Tuple[bool, str, int]:
    """`networksetup -getwebproxy <svc>` 출력 → (enabled, server, port)."""
    enabled, server, port = False, "", 0
    for line in text.splitlines():
        key, _, value = line.partition(":")
        key, value = key.strip(), value.strip()
        if key == "Enabled":
            enabled = value == "Yes"
        elif key == "Server":
            server = value
        elif key == "Port":
            port = int(value) if value.isdigit() else 0
    return enabled, server, port


def _is_set(runner: Runner, getter: str, svc: str, host: str, port: int) -> bool:
    enabled, server, current_port = parse_proxy(runner(["networksetup", getter, svc]))
    return enabled and server == host and current_port == port


def ensure_all(host: str, port: int, runner: Runner = run) -> Tuple[List[str], List[str]]:
    """모든 활성 서비스의 HTTP·HTTPS 프록시를 host:port로 맞춘다.

    setter 뒤 getter로 재확인해 (실제 바뀐 항목, 설정이 안 먹힌 항목)을 돌려준다.
    """
    changed, failed = [], []
    for svc in parse_services(runner(["networksetup", "-listallnetworkservices"])):
        for getter, setter, _ in _PAIRS:
            if _is_set(runner, getter, svc, host, port):
                continue
            runner(["networksetup", setter, svc, host, str(port)])
            label = "%s %s" % (svc, setter)
            (changed if _is_set(runner, getter, svc, host, port) else failed).append(label)
    return changed, failed


def clear_all(runner: Runner = run) -> None:
    for svc in parse_services(runner(["networksetup", "-listallnetworkservices"])):
        for _, _, state_setter in _PAIRS:
            runner(["networksetup", state_setter, svc, "off"])
```

- [ ] **Step 4: 통과 확인**

Run: `python3 -m pytest tests/test_proxyconf.py -q`
Expected: 모두 PASS

- [ ] **Step 5: 커밋**

```bash
git add engine/impel/proxyconf.py engine/tests/test_proxyconf.py
git commit -m "feat(engine): 시스템 프록시 설정 파싱·유지·해제"
```

---

## S3 — 애드온·설치 (`feat/engine-addon`)

### Task 9: hosts 미러·프록시 재확인 루프 (`impel/keeper.py`)

hosts 미러와 시스템 프록시 재확인을 tick 단위로 하는 순수 클래스. `addon.py`는 1초마다 `tick()`만 부른다. hosts 쓰기 함수와 프록시 설정 함수를 인자로 받으므로 시스템을 건드리지 않고 테스트한다.

**Files:**
- Create: `engine/impel/keeper.py`
- Test: `engine/tests/test_keeper.py`

**Interfaces:**
- Consumes: `RulesFile` (Task 4), `Rules.mirror_hosts` (Task 3), `hosts.write_hosts` (Task 7), `proxyconf.ensure_all` (Task 8)
- Produces: `PROXY_TICK = 30`, `class Keeper(rules_file: RulesFile, port: int, write_hosts=None, ensure_proxy=None, log=None)` with `.tick() -> None` (한 주기. hosts와 프록시를 따로 감싸 한쪽 오류가 다른 쪽을 막지 않는다. 같은 오류는 한 번만 로그).

- [ ] **Step 1: 실패하는 테스트**

`engine/tests/test_keeper.py`:

```python
import os
import time

from impel.keeper import Keeper, PROXY_TICK
from impel.rules import RulesFile

ALWAYS = '{"version": 1, "groups": [{"id": "a", "title": "a", "addresses": ["a.example"], "open": {}}]}'
OTHER = '{"version": 1, "groups": [{"id": "b", "title": "b", "addresses": ["b.example"], "open": {}}]}'


class Fakes:
    def __init__(self, ensure_result=([], [])):
        self.hosts_writes = []
        self.ensure_calls = []
        self.logs = []
        self.ensure_result = ensure_result

    def write_hosts(self, hosts):
        self.hosts_writes.append(list(hosts))
        return True

    def ensure(self, host, port):
        self.ensure_calls.append((host, port))
        return self.ensure_result

    def log(self, msg):
        self.logs.append(msg)


def _write(path, text):
    path.write_text(text, encoding="utf-8")
    t = time.time() + 2
    os.utime(str(path), (t, t))


def _keeper(tmp_path, fakes, text=None):
    p = tmp_path / "rules.json"
    if text is not None:
        _write(p, text)
    return Keeper(RulesFile(str(p)), 8899,
                  write_hosts=fakes.write_hosts, ensure_proxy=fakes.ensure, log=fakes.log)


def test_no_hosts_write_when_rules_missing(tmp_path):
    f = Fakes()
    _keeper(tmp_path, f).tick()
    assert f.hosts_writes == []


def test_no_hosts_write_when_rules_broken(tmp_path):
    f = Fakes()
    _keeper(tmp_path, f, "{ broken").tick()
    assert f.hosts_writes == []


def test_hosts_written_once_per_rules_version(tmp_path):
    f = Fakes()
    k = _keeper(tmp_path, f, ALWAYS)
    k.tick()
    k.tick()
    assert f.hosts_writes == [["a.example", "www.a.example"]]


def test_hosts_rewritten_when_rules_change(tmp_path):
    f = Fakes()
    k = _keeper(tmp_path, f, ALWAYS)
    k.tick()
    _write(tmp_path / "rules.json", OTHER)
    k.tick()
    assert f.hosts_writes == [["a.example", "www.a.example"], ["b.example", "www.b.example"]]


def test_hosts_kept_when_rules_break_later(tmp_path):
    f = Fakes()
    k = _keeper(tmp_path, f, ALWAYS)
    k.tick()
    _write(tmp_path / "rules.json", "{ broken")
    k.tick()
    assert f.hosts_writes == [["a.example", "www.a.example"]]  # 깨진 파일로는 미러를 지우지 않는다


def test_proxy_ensured_every_30_ticks(tmp_path):
    f = Fakes()
    k = _keeper(tmp_path, f, ALWAYS)
    for _ in range(PROXY_TICK * 2):
        k.tick()
    assert f.ensure_calls == [("127.0.0.1", 8899)] * 2


def test_proxy_failure_logged_once_while_it_persists(tmp_path):
    f = Fakes(ensure_result=([], ["Wi-Fi -setwebproxy"]))
    k = _keeper(tmp_path, f, ALWAYS)
    for _ in range(PROXY_TICK * 2 + 1):
        k.tick()
    assert [m for m in f.logs if "could not be set" in m] == ["system proxy could not be set: Wi-Fi -setwebproxy"]


def test_proxy_still_ensured_when_hosts_write_keeps_failing(tmp_path):
    # hosts가 막혀도 프록시 재확인은 돌아야 하고, 같은 오류는 한 번만 로그한다
    def boom(hosts):
        raise RuntimeError("read-only")
    f = Fakes()
    p = tmp_path / "rules.json"
    _write(p, ALWAYS)
    k = Keeper(RulesFile(str(p)), 8899, write_hosts=boom, ensure_proxy=f.ensure, log=f.log)
    for _ in range(PROXY_TICK * 3):
        k.tick()
    assert f.ensure_calls == [("127.0.0.1", 8899)] * 3
    assert [m for m in f.logs if "hosts error" in m] == ["hosts error: read-only"]


def test_proxy_exception_logged_once_and_hosts_unaffected(tmp_path):
    def boom(host, port):
        raise RuntimeError("networksetup missing")
    f = Fakes()
    p = tmp_path / "rules.json"
    _write(p, ALWAYS)
    k = Keeper(RulesFile(str(p)), 8899, write_hosts=f.write_hosts, ensure_proxy=boom, log=f.log)
    for _ in range(PROXY_TICK * 2 + 1):
        k.tick()
    assert f.hosts_writes == [["a.example", "www.a.example"]]
    assert [m for m in f.logs if "proxy error" in m] == ["proxy error: networksetup missing"]
```

- [ ] **Step 2: 실패 확인**

Run: `python3 -m pytest tests/test_keeper.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'impel.keeper'`

- [ ] **Step 3: 구현**

`engine/impel/keeper.py`:

```python
"""hosts 미러·프록시 재확인 루프. addon.py가 1초마다 tick()을 부른다.

- 규칙 파일이 성공적으로 다시 읽혔을 때만 hosts 미러를 갱신한다 (파일 없음·깨짐엔 손대지 않는다).
- PROXY_TICK 주기마다 시스템 프록시를 재확인한다. 실패는 상태가 바뀔 때 한 번만 로그.
mitmproxy에 의존하지 않는다.
"""
from typing import Callable, List, Optional, Set, Tuple

from . import hosts, proxyconf
from .rules import RulesFile

PROXY_TICK = 30

WriteHosts = Callable[[List[str]], bool]
EnsureProxy = Callable[[str, int], Tuple[List[str], List[str]]]
Log = Callable[[str], None]


class Keeper:
    def __init__(self, rules_file: RulesFile, port: int,
                 write_hosts: Optional[WriteHosts] = None,
                 ensure_proxy: Optional[EnsureProxy] = None,
                 log: Optional[Log] = None):
        self.rules_file = rules_file
        self.port = port
        self._write_hosts = write_hosts or hosts.write_hosts
        self._ensure_proxy = ensure_proxy or proxyconf.ensure_all
        self._log = log or (lambda msg: None)
        self._synced_version = 0
        self._ticks = 0
        self._proxy_failed = set()  # type: Set[str]
        self._hosts_error = None  # type: Optional[str]
        self._proxy_error = None  # type: Optional[str]

    def tick(self) -> None:
        """한 주기. hosts와 프록시를 따로 감싼다 — hosts가 막혀도 프록시 재확인은 돈다.

        예외는 삼키고 로그로 남기되, 같은 오류가 이어지는 동안엔 한 번만 남긴다.
        """
        self._hosts_error = self._guard("hosts", self._sync_hosts, self._hosts_error)
        if self._ticks % PROXY_TICK == 0:
            self._proxy_error = self._guard("proxy", self._sync_proxy, self._proxy_error)
        self._ticks += 1

    def _guard(self, name: str, fn: Callable[[], None], last_error: Optional[str]) -> Optional[str]:
        """fn을 실행하고 오류 메시지를 돌려준다(성공이면 None). 직전과 같은 메시지는 로그하지 않는다."""
        try:
            fn()
            return None
        except Exception as e:
            msg = "%s error: %s" % (name, e)
            if msg != last_error:
                self._log(msg)
            return msg

    def _sync_hosts(self) -> None:
        rules = self.rules_file.current()
        version = self.rules_file.version
        if version < 1 or version == self._synced_version:
            return
        mirror = rules.mirror_hosts()
        if self._write_hosts(mirror):
            self._log("hosts block updated (%d hosts)" % len(mirror))
        self._synced_version = version

    def _sync_proxy(self) -> None:
        changed, failed = self._ensure_proxy("127.0.0.1", self.port)
        if changed:
            self._log("system proxy re-applied: %s" % ", ".join(changed))
        for item in failed:
            if item not in self._proxy_failed:
                self._log("system proxy could not be set: %s" % item)
        self._proxy_failed = set(failed)
```

- [ ] **Step 4: 통과 확인**

Run: `python3 -m pytest tests/test_keeper.py -q`
Expected: 모두 PASS

- [ ] **Step 5: 커밋**

```bash
git add engine/impel/keeper.py engine/tests/test_keeper.py
git commit -m "feat(engine): hosts 미러·프록시 재확인 루프 — 성공한 규칙에만 hosts 미러, 프록시 실패는 한 번만 로그"
```

---

### Task 10: mitmproxy 애드온 (`addon.py`) + 통합 테스트

**Files:**
- Create: `engine/addon.py`
- Test: `engine/tests/test_integration.py`

**Interfaces:**
- Consumes: `RulesFile`, `Rules.decide/intercept_host`, `notice.render`, `Keeper.tick`
- 환경변수: `IMPEL_RULES` (규칙 파일 절대 경로, 필수), `IMPEL_PORT` (기본 8899), `IMPEL_NO_KEEPER` (있으면 hosts·프록시 루프를 돌리지 않는다. 통합 테스트가 시스템을 건드리지 않게 하기 위한 것).
- SNI 속성 표기는 Task 0 Step 2에서 통한 것을 쓴다 (아래 코드는 `data.context.client.sni`).

- [ ] **Step 1: 애드온 구현**

`engine/addon.py`:

```python
"""Impel Down mitmproxy 애드온. 훅 3개를 impel/ 모듈에 연결한다.

실행: mitmdump --listen-host 127.0.0.1 --listen-port 8899 --set connection_strategy=lazy -s addon.py
환경변수: IMPEL_RULES(필수) · IMPEL_PORT(기본 8899) · IMPEL_NO_KEEPER(테스트용)
"""
import asyncio
import logging
import os
import sys
from datetime import datetime

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from mitmproxy import http, tls  # noqa: E402

from impel import notice  # noqa: E402
from impel.keeper import Keeper  # noqa: E402
from impel.rules import RulesFile  # noqa: E402

log = logging.getLogger("impel")

RULES_PATH = os.environ.get("IMPEL_RULES", "")
PORT = int(os.environ.get("IMPEL_PORT", "8899"))
NO_KEEPER = bool(os.environ.get("IMPEL_NO_KEEPER"))


class ImpelDown:
    def __init__(self):
        if not RULES_PATH:
            raise RuntimeError("IMPEL_RULES 환경변수가 필요하다")
        self.rules_file = RulesFile(RULES_PATH, log=log.warning)
        self.keeper = Keeper(self.rules_file, PORT, log=log.info)

    # ---- 생명주기 ----
    def running(self):
        log.info("impel-down: rules=%s port=%d keeper=%s", RULES_PATH, PORT, not NO_KEEPER)
        if not NO_KEEPER:
            asyncio.get_running_loop().create_task(self._run_keeper())

    async def _run_keeper(self):
        while True:
            self.keeper.tick()
            await asyncio.sleep(1)

    # ---- 판정 ----
    def tls_clienthello(self, data: tls.ClientHelloData):
        """규칙 호스트만 복호화, 나머지는 터널 통과."""
        sni = data.context.client.sni or ""
        if not sni or not self.rules_file.current().intercept_host(sni):
            data.ignore_connection = True

    def request(self, flow: http.HTTPFlow):
        rules = self.rules_file.current()
        host = flow.request.pretty_host.lower()
        path = flow.request.path.split("?", 1)[0]
        now = datetime.now()
        group = rules.decide(host, path, now)
        if group is None:
            return
        body = notice.render(group.title, group.next_open(now), host)
        flow.response = http.Response.make(
            403, body.encode("utf-8"), {"Content-Type": "text/html; charset=utf-8"}
        )


addons = [ImpelDown()]
```

- [ ] **Step 2: 통합 테스트** (`IMPEL_MITMDUMP=<mitmdump 경로>`가 있을 때만)

`engine/tests/test_integration.py`:

```python
"""실제 mitmdump로 애드온을 띄워 curl로 확인한다. IMPEL_MITMDUMP가 없으면 건너뛴다."""
import json
import os
import socket
import subprocess
import time
from datetime import datetime

import pytest

from impel.rules import DAY_KEYS

MITMDUMP = os.environ.get("IMPEL_MITMDUMP")
pytestmark = pytest.mark.skipif(not MITMDUMP, reason="IMPEL_MITMDUMP not set")
ADDON = os.path.join(os.path.dirname(__file__), "..", "addon.py")
PORT = 18899


def _wait_port(port, timeout=15):
    deadline = time.time() + timeout
    while time.time() < deadline:
        with socket.socket() as s:
            if s.connect_ex(("127.0.0.1", port)) == 0:
                return
        time.sleep(0.2)
    raise RuntimeError("proxy did not start")


def _curl(url, cacert):
    out = subprocess.run(
        ["curl", "-s", "--max-time", "20", "-x", "127.0.0.1:%d" % PORT, "--cacert", cacert,
         "-o", "/dev/stdout", "-w", "\n__CODE__%{http_code}", url],
        capture_output=True, text=True)
    body, _, code = out.stdout.rpartition("\n__CODE__")
    return code, body


@pytest.fixture
def engine(tmp_path):
    rules = tmp_path / "rules.json"
    now_key = DAY_KEYS[datetime.now().weekday()]
    rules.write_text(json.dumps({"version": 1, "groups": [
        {"id": "blocked", "title": "막힘", "addresses": ["community.example"], "open": {}},
        {"id": "open", "title": "열림", "addresses": ["example.com"],
         "open": {now_key: list(range(24))}},
    ]}), encoding="utf-8")
    conf = tmp_path / "conf"
    env = dict(os.environ, IMPEL_RULES=str(rules), IMPEL_PORT=str(PORT), IMPEL_NO_KEEPER="1")
    proc = subprocess.Popen(
        [MITMDUMP, "--listen-host", "127.0.0.1", "--listen-port", str(PORT),
         "--set", "confdir=%s" % conf, "--set", "connection_strategy=lazy", "-s", ADDON],
        env=env, stdout=subprocess.PIPE, stderr=subprocess.STDOUT, text=True)
    try:
        _wait_port(PORT)
        for _ in range(50):
            if (conf / "mitmproxy-ca-cert.pem").exists():
                break
            time.sleep(0.2)
        yield str(conf / "mitmproxy-ca-cert.pem")
    finally:
        proc.terminate()
        proc.wait(timeout=10)


def test_blocked_host_gets_notice(engine):
    code, body = _curl("https://community.example/anything?x=1", engine)
    assert code == "403"
    assert "정신 차리고 중요한 일에 집중하세요." in body and "막힘" in body


def test_open_host_passes_through_decrypted(engine):
    code, _ = _curl("https://example.com/", engine)
    assert code == "200"


def test_unlisted_host_tunnels_without_cert(engine):
    # 규칙에 없는 호스트는 복호화하지 않으므로 인증서 없이도 성공한다
    out = subprocess.run(["curl", "-s", "--max-time", "20", "-x", "127.0.0.1:%d" % PORT,
                          "-o", "/dev/null", "-w", "%{http_code}", "https://www.apple.com/"],
                         capture_output=True, text=True)
    assert out.stdout == "200"
```

- [ ] **Step 3: 단위 테스트 전체 + 통합 테스트**

```bash
cd engine
python3 -m pytest -q                          # 통합은 skip
IMPEL_MITMDUMP=/tmp/impel-spike/<Task 0에서 기록한 mitmdump 경로> python3 -m pytest tests/test_integration.py -q -v
```

Expected: 단위 전부 PASS, 통합 3개 PASS.
통합이 실패하면 mitmdump 출력을 보고 원인을 잡는다. `finally`에서 `proc.stdout.read()`를 찍도록 임시로 추가하면 된다.

- [ ] **Step 4: 커밋**

```bash
git add engine/addon.py engine/tests/test_integration.py
git commit -m "feat(engine): mitmproxy 애드온 — 규칙 호스트만 복호화, 차단 시 안내 페이지, 재확인 루프 연결"
```

---

### Task 11: LaunchDaemon·설치·제거 스크립트 + 실기 설치

**Files:**
- Create: `engine/com.impel-down.engine.plist`, `engine/install.sh`, `engine/uninstall.sh`, `engine/README.md`

- [ ] **Step 1: plist 템플릿**

`engine/com.impel-down.engine.plist` (`__RULES__`는 install.sh가 치환):

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key><string>com.impel-down.engine</string>
  <key>ProgramArguments</key>
  <array>
    <string>/Library/Application Support/Impel Down/mitmproxy/mitmdump</string>
    <string>--listen-host</string><string>127.0.0.1</string>
    <string>--listen-port</string><string>8899</string>
    <string>--set</string><string>confdir=/Library/Application Support/Impel Down/conf</string>
    <string>--set</string><string>connection_strategy=lazy</string>
    <string>--set</string><string>flow_detail=0</string>
    <string>-s</string><string>/Library/Application Support/Impel Down/addon.py</string>
  </array>
  <key>EnvironmentVariables</key>
  <dict>
    <key>IMPEL_RULES</key><string>__RULES__</string>
    <key>IMPEL_PORT</key><string>8899</string>
  </dict>
  <key>RunAtLoad</key><true/>
  <key>KeepAlive</key><true/>
  <key>ThrottleInterval</key><integer>10</integer>
  <key>StandardOutPath</key><string>/Library/Logs/impel-down.log</string>
  <key>StandardErrorPath</key><string>/Library/Logs/impel-down.err.log</string>
</dict>
</plist>
```

- [ ] **Step 2: install.sh**

```sh
#!/bin/sh
# Impel Down 엔진 설치. 실행: sudo sh engine/install.sh
set -eu
[ "$(id -u)" -eq 0 ] || { echo "sudo로 실행해야 한다"; exit 1; }
USER_NAME="${SUDO_USER:-}"
[ -n "$USER_NAME" ] || { echo "SUDO_USER를 알 수 없다 — sudo로 실행"; exit 1; }
USER_HOME=$(dscl . -read "/Users/$USER_NAME" NFSHomeDirectory | awk '{print $2}')
RULES="$USER_HOME/Library/Application Support/Impel Down/rules.json"

APP_DIR="/Library/Application Support/Impel Down"
PLIST=/Library/LaunchDaemons/com.impel-down.engine.plist
MITM_VER=12.2.3
MITM_SHA256=""   # S0(Task 0 Step 1)에서 잰 arm64 tarball sha256. 비어 있으면 설치를 거부한다.
ARCH=$(uname -m)
TARBALL="mitmproxy-$MITM_VER-macos-$ARCH.tar.gz"
URL="https://downloads.mitmproxy.org/$MITM_VER/$TARBALL"
HERE=$(cd "$(dirname "$0")" && pwd)

[ -n "$MITM_SHA256" ] || { echo "MITM_SHA256이 비어 있다 — S0에서 잰 값을 install.sh에 넣어라"; exit 1; }
mkdir -p "$APP_DIR/mitmproxy" "$APP_DIR/conf"

# 1. mitmproxy 실행 파일 (없을 때만 받는다). root가 실행할 파일이라 sha256을 대조한다.
if [ ! -x "$APP_DIR/mitmproxy/mitmdump" ]; then
  TMP=$(mktemp -d)
  echo "mitmproxy $MITM_VER 받는 중…"
  curl -fsSL "$URL" -o "$TMP/$TARBALL"
  echo "$MITM_SHA256  $TMP/$TARBALL" | shasum -a 256 -c - || { echo "sha256 불일치 — 설치 중단"; rm -rf "$TMP"; exit 1; }
  tar -xzf "$TMP/$TARBALL" -C "$TMP"
  MITMDUMP=$(find "$TMP" -type f -name mitmdump | head -1)
  [ -n "$MITMDUMP" ] || { echo "tarball 안에 mitmdump가 없다"; rm -rf "$TMP"; exit 1; }
  cp -R "$(dirname "$MITMDUMP")/." "$APP_DIR/mitmproxy/"
  rm -rf "$TMP"
fi
[ -x "$APP_DIR/mitmproxy/mitmdump" ] || { echo "mitmdump 실행 불가"; exit 1; }

# 2. 애드온 복사, root 소유
rm -rf "$APP_DIR/impel"
cp "$HERE/addon.py" "$APP_DIR/addon.py"
cp -R "$HERE/impel" "$APP_DIR/impel"
find "$APP_DIR/impel" -name __pycache__ -prune -exec rm -rf {} + 2>/dev/null || true
chown -R root:wheel "$APP_DIR"
chmod -R go-w "$APP_DIR"
chmod 700 "$APP_DIR/conf"

# 3. LaunchDaemon
sed -e "s|__RULES__|$RULES|g" "$HERE/com.impel-down.engine.plist" > "$PLIST"
chown root:wheel "$PLIST"; chmod 644 "$PLIST"
launchctl bootout system "$PLIST" 2>/dev/null || true
launchctl bootstrap system "$PLIST"

# 4. 인증서 신뢰 (엔진 첫 실행이 만든다 — 최대 30초 대기)
CERT="$APP_DIR/conf/mitmproxy-ca-cert.pem"
i=0
while [ ! -f "$CERT" ] && [ "$i" -lt 30 ]; do sleep 1; i=$((i+1)); done
if [ ! -f "$CERT" ]; then
  echo "인증서가 30초 안에 생기지 않았다 — 설치 실패. 로그: /Library/Logs/impel-down.err.log"
  launchctl bootout system "$PLIST" || true
  exit 1
fi
security add-trusted-cert -d -r trustRoot -k /Library/Keychains/System.keychain "$CERT"

echo "설치 완료. 시스템 프록시는 엔진이 30초 안에 잡는다."
echo "규칙 파일: $RULES"
echo "로그: /Library/Logs/impel-down.log · impel-down.err.log"
```

- [ ] **Step 3: uninstall.sh**

```sh
#!/bin/sh
# Impel Down 엔진 제거. 실행: sudo sh engine/uninstall.sh  (규칙 파일은 남긴다)
set -u
[ "$(id -u)" -eq 0 ] || { echo "sudo로 실행해야 한다"; exit 1; }
APP_DIR="/Library/Application Support/Impel Down"
PLIST=/Library/LaunchDaemons/com.impel-down.engine.plist

# 1. 서비스부터 내린다 — 재확인 루프가 살아 있으면 프록시를 도로 잡는다
launchctl bootout system "$PLIST" 2>/dev/null || true
rm -f "$PLIST"

# 2. 프록시 설정 해제 · hosts 블록 제거 — 설치된 모듈로 (셸에 같은 로직을 다시 쓰지 않는다)
if [ -d "$APP_DIR/impel" ]; then
  PYTHONPATH="$APP_DIR" /usr/bin/python3 -c "from impel import proxyconf, hosts; proxyconf.clear_all(); hosts.write_hosts([])"
fi

# 3. 인증서 신뢰 해제
CERT="$APP_DIR/conf/mitmproxy-ca-cert.pem"
[ -f "$CERT" ] && security remove-trusted-cert -d "$CERT" 2>/dev/null || true

# 4. 파일 제거
rm -rf "$APP_DIR"
echo "제거 완료. 규칙 파일은 남아 있다."
```

- [ ] **Step 4: README**

`engine/README.md`:

```markdown
# engine

- 설치: `sudo sh engine/install.sh` — mitmproxy 실행 파일을 받아 `/Library/Application Support/Impel Down/`에 두고, LaunchDaemon 등록, 인증서 신뢰까지.
- 제거: `sudo sh engine/uninstall.sh`
- 상태: `sudo launchctl print system/com.impel-down.engine | head`
- 로그: `/Library/Logs/impel-down.log` · `impel-down.err.log`
- 규칙 파일: `~/Library/Application Support/Impel Down/rules.json` (앱이 쓴다. 형식은 `docs/fixtures/rules.json`)
- 테스트: `cd engine && python3 -m pytest -q` · 통합은 `IMPEL_MITMDUMP=<mitmdump 경로> python3 -m pytest tests/test_integration.py` (통과 케이스 둘은 외부 네트워크가 필요하다)
```

- [ ] **Step 5: 실기 설치·검증 (보스가 sudo 실행)**

```bash
sudo sh engine/install.sh   # repo 루트에서
sudo launchctl print system/com.impel-down.engine | grep -E "state|pid"
sleep 35 && networksetup -getsecurewebproxy Wi-Fi          # Enabled: Yes / 127.0.0.1 / 8899
```

규칙 파일을 손으로 하나 만들어 본다. 앱이 아직 없어 임시이고, repo 밖 파일이라 실제 쓰는 값을 넣어도 된다:

```bash
mkdir -p "$HOME/Library/Application Support/Impel Down"
cat > "$HOME/Library/Application Support/Impel Down/rules.json" <<'EOF'
{"version": 1, "groups": [{"id": "test", "title": "테스트", "addresses": ["example.com"], "open": {}}]}
EOF
sleep 2 && grep -A3 "# impel-down" /etc/hosts          # 미러 블록에 example.com·www.example.com
# curl은 시스템 프록시를 안 타므로 -x로 지정. 인증서는 시스템 키체인 신뢰를 탄다 (안 타면 -k로 코드만 확인)
curl -s -x 127.0.0.1:8899 -o /dev/null -w "%{http_code}\n" https://example.com/   # 403
```

Safari·Chrome으로 `https://example.com` → 안내 페이지 확인. 이전에 손으로 넣어둔 `# site-block` hosts 블록이 있으면, 앱에서 같은 사이트를 등록한 뒤 `sudo`로 지운다.

- [ ] **Step 6: 커밋**

```bash
git add engine/com.impel-down.engine.plist engine/install.sh engine/uninstall.sh engine/README.md
git commit -m "feat(engine): LaunchDaemon plist, 설치·제거 스크립트"
```

---

## 자체 점검

- spec 「엔진」: 프록시(Task 10) · 복호화 범위(`tls_clienthello`) · 차단 응답 403+HTML(Task 6·10) · 시스템 프록시 유지(Task 8·9) · hosts 관리(Task 7·9) · 실행 주체·권한(Task 11 plist, root 소유 복사, sha256 대조, `KeepAlive`·10초).
- spec 「규칙 파일」: 위치·원자적 교체는 앱 몫 · 1초 재로드와 실패 시 유지(Task 4, 깨진 파일로 미러를 지우지 않는 건 Task 9) · 스키마 검증(Task 3) · 주소·시간 뜻(Task 1·2) · 판정 시각 인자(모든 판정 함수가 `at` 인자) · 여러 묶음 판정(Task 5 `decide` 기대값).
- spec 「설치·제거」: Task 11. uninstall은 `bootout`부터, 프록시 해제·hosts 제거는 설치된 모듈로. 인증서 30초 대기·실패 처리.
- spec 「테스트」: 단위(Task 1–9) · 계약 픽스처(Task 5) · 통합 curl 셋(Task 10).
- spec 「구현 전 실측」 일곱 항목: Task 0 (tarball sha256·구조·기동, 연결별 통과와 SNI 표기, 서비스 넷 프록시 적용, 엔진 죽은 상태, 인증 창, Safari·Private Relay, QUIC, 브라우저 외 앱).
- 앱 plan으로 넘기는 것: 규칙 파일 쓰기(원자적 교체), 붙여넣기 정리(`normalize_address`와 같은 규칙), 상태 칩·시간표 요약(`expectations.json`의 `summary`, 요일 줄은 월요일부터), 카드 상태는 묶음 기준이고 실제 차단은 묶음 합집합, 로그인 자동 실행.
